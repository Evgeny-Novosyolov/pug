- let navList= ["Главная", "Блог", "Портфолио", "Контакты"]
- let aLink = ["google.ru", "1213", "3333", "43242"]

mixin menu(menu_items)
  ul(class="menu__list")
  each item in menu_items
    li.menu__item: a.menu-link(href=item.href class={active: currentURL ===  item.href}) #{item.title}


nav.navbar
  +menu([{'title': 'Главная', 'href' : 'index.html'}, {'title': 'Блог', 'href' : 'blog.html'},{'title': 'Портфолио', 'href' : 'olol.html'},{'title': 'Контакты', 'href' : 'contact.html'}, ])




mixin block(title, ...text1)
  .block__wrapper
    .box
      h1 #{title}
      .box__main
      ul.box__list
        each itemText in text1 
          li.box__item #{itemText}
         
     


block blockWrapper
  .block__wrapper2


mixin boxItem(title, ...text)
  .box
    h1 #{title}
    ul.box__list
      each itemText in text
        li.box__item #{itemText}   




mixin boxItem2(event)
  .box
    h1 #{event.title}
    ul.box__list
      each itemText in event.text
        li.box__item #{itemText}   



mixin boxItem3(event)
  each eventItem in event
    .box
      h1 #{eventItem.title}
      ul.box__list
      each itemText in eventItem.text
        li.box__item #{itemText}   
      


mixin movie-card(movie)
  div.movie-card
    a(href=movie.imdbURL)
      h2.movie-title= movie.title
    img.movie-poster(src=movie.posterImage)
    h3 Cast
    ul.cast
      each actor in movie.cast
        li= actor
    div.rating
      if movie.rating > 5
        img(src="img/thumbs-up")
      else
        img(src="img/thumbs-down")
    ul.genre
      each genre in movie.genres
        li= genre


